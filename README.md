# CopilotKit AI 助手

一个基于 Next.js + CopilotKit 的 AI 助手示例项目。

## 1. 安装依赖

```bash
npm install
```

## 2. 配置环境变量

```bash
cp .env.example .env.local
```

将 `GOOGLE_API_KEY` 替换为你的真实 Gemini Key。

## 3. 启动开发环境

```bash
npm run dev
```

打开 `http://localhost:3000` 查看聊天助手。
